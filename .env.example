# Base URL used in QR codes (no trailing slash)
BASE_URL=http://localhost:8000

# Database URL (SQLite for local dev)
DATABASE_URL=sqlite:///./food_storage.db

# Secret key for session signing (generate a random one for production)
SECRET_KEY=change-me-to-a-random-secret

# Upload directory for photos
UPLOAD_DIR=./uploads
